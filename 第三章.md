$env:HTTP_PROXY="http://127.0.0.1:7897"
$env:HTTPS_PROXY="http://127.0.0.1:7897"
# 前两章总结
第一章，我们把几何图形的求积问题转化为寻找图形与其外接直边图形的比(简称求积系数)，运用了几何方法；第二章，基于小矩形近似的想法，把图形与其外接直边图形的比转化成序列和与前者最大项构成的等项数序列和之比。注意最大项，等项数，这两个关键特征
阅读drive.py
你可以创个新文件用于记录分式手动排版的参数，我认为需要记录目前分子分母与分数线的间隔，分数线超出最长部分(分子/分母)的长度，以及符号与单个项的间隔，还有分子分母间的符号需要上下。对齐的规则。还有一旦涉及手动排版，注意不要总是反复纠结哪个位置更好，你只需算个大概，最终我兜底调整好所有位置。要告诉我如何调整公式的位置。以后遇到分式手动排版就按这个来。接下来在drive.py的末尾续写动画。由于图中公式无法直接计算，因为是根式，次数为二分之一，那我们退而求其次，计算我们能计算的。接下来计算的是，去掉根号的情况先标记顶部公式的指数。获取n→∞的位置，算出n→∞与中央公式的间隔d，然后在顶部公式右侧间隔d的位置FadeIn m=1/2。然后在m=1/2下方两格，FadeIn
m=1，获取顶部公式的中心位置，该位置往下两格就是m=1的公式的中心，它最终长成这样：R^2-(0s)^2 + R^2-(1s)^2 + R^2-(2s)^2 + ··· + R^2-((n-1)s)^2/R^2 + R^2 + R^2 + ··· + R^2，你需要根据中心和公式提前计算好最终各个元素的位置。为了让观众能够看懂公式构造的过程，并不是一上来就直接显示最终公式，需要先FadeIn R^2-(0s)^2 + R^2-(1s)^2 + R^2-(2s)^2 + ··· +

R^2-((n-1)s)^2,然后把R^2-(0s)^2 变成R^2，然后学习一个发牌式动画的写法，上述某些内容你看能不能整理skills

⎿  [Image #6]

我想告诉你，我们正在完成一个伟大的事情。我在这之前已经做完了关于沃里斯乘积的第一章视频和第二章视频，现在你负责的是第三章视频的manim素材生成。第一章，我们把几何图形的求积问题转化为寻找图形与其外接直边图形的比(简称求积系数)，运用了几何方法；第二章，基于小矩形近似的想法，把图形与其外接直边图形的比转化成序列和与前者最大项构成的等项数序列和之比。而我们第三章的目标，就是推出Wallis乘积。我使用的是Wallis的原始思路，基于他的无穷算数(是的我看了两个月。)
图形与其外接直边图形的比例，等于序列和与序列中最大项构成的等项数序列和。比如0+1+2/2+2+2,0+1+2+3/3+3+3+3，以此类推，增加序列幂次m，增加序列项数n，先前我们就是这样，算出了很多很多结论。但是，只有当m为整数时，才有规律。比如当m=1/2时，是一系列根式相加，根本无法看出什么。但是根据从整数幂次中归纳出的结论，求积系数=1/m+1，把m=1/2带入后，就能得到2/3，根式幂趋于∞时的系数就出现了。以上大概是我第二章在视频中讲的内容。面对根式求和的情况，沃利斯使用旁敲侧击的方式间接得到的。所以第三章，面对我们的m=1/2，这个代表了圆的求积系数(pi/4)的式子，我们先去想象能计算的是什么。是不是当m=整数时，就可以计算了？所以接下来我们要探讨，先探讨m=1的情况。我这么说你能理解吗？准备好了就说一声。


 可以，接下来先在[16,3,0]的位置FadeIn m=1，然后在[5.5,3,0]的位置根据 @fraction_layout.md
  提前规划好这个指数为1的新分式，这次我手写给你，你要理解为什么m=1的情况下是这个公式：R^2-(0s)^2 + R^2-(1s)^2 + R^2-(2s)^2 + ··· +  R^2-((n-1)s)^2/R^2 + R^2 + R^2 + ··· + R^2 

暂时没有。我得等pqh渲染后才能直到效果。但是呢，不是一上来就直接FadeIn 这整个m=1的式子。为了让观众理解，需要先列出公式的分子部分，然后把分子项R^2-(0s)^2 变成R^2 ，然后帮我设计一个丝滑的发牌式动画效果，对R^2参考把分子的第一项R^2丝滑发到分母序列R^2 + R^2 + R^2 + ··· + R^2 ，组成完整分式后，将分子的第一个R^2还原为R^2-(0s)^2。以下是发牌式动画的方法，可参考:

1. **源对象隔离 (Source Isolation)**：

- 使用 `MathTex` 构建公式时，必须将“作为发牌源头的元素”作为独立的字符串参数传入。

- 示例：`source_tex = MathTex(r"前置公式...", r"核心元素", r"...后置公式")`，以便通过索引 `source_tex[1]` 独立调用。

2. **目标阵列提取 (Target Grouping)**：

- 构建目标序列（如分母）后，必须使用列表推导式或切片，将需要飞入的“坑位”元素单独提取成一个 `VGroup`。

- 示例：`targets = VGroup(*[target_tex[i] for i in 目标索引列表])`。

3. **构建动画列表 (Animation Array)**：

- 必须使用列表推导式结合 `TransformFromCopy`，生成从源元素到所有目标元素的动画列表。

- 示例：`animations = [TransformFromCopy(source_tex[1], t) for t in targets]`。

4. **错位播放 (Staggered Playback)**：

- 在 `self.play` 中使用 `*` 解包动画列表。

- **核心重点**：必须添加 `lag_ratio=0.1` (或 0.05~0.2 之间的小数) 参数，利用时间差制造出发牌式的丝滑视觉效果。

- 示例：`self.play(*animations, run_time=2, lag_ratio=0.1)`。

请根据上述逻辑，帮我实现从 [源元素X] 飞入组成 [目标序列Y] 的代码。